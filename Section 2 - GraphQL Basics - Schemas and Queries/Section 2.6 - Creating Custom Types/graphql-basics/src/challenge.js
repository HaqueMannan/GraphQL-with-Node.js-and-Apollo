// 1. Create a Post type
// 2. Add id, title (type: string), body (type: string) and published (type: boolean) to the Post type (all non-nullable)
// 3. Define a "post" query that returns a single post
// 4. Setup the resolver method to return some post data
// 5. Test out the query in GraphQL Playground (run npm run start command to test on localhost:4000)