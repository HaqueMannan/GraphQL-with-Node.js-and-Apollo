// 1. Setup an array of three posts with dummy post data (id, title, body, published)
// 2. Setup a "posts" query and resolver that returns all the posts
// 3. Add a "query" operation argument that only returns post that contain the query string in the title or body
// 4. Run a few sample queries searching for posts with a specific title in the GraphQL Playground (run the terminal command npm run start and in the browser go to localhost:4000)